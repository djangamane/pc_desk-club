{
  "framework": "vite",
  "buildCommand": "tsc -p tsconfig.vercel.json && vite build --config vite.vercel.config.ts",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "github": {
    "silent": true
  }
}