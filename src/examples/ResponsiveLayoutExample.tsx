import React from 'react';

const ResponsiveLayoutExample: React.FC = () => {
  return (
    <div style={{ padding: '20px' }}>
      <h2>Responsive Layout Example (Disabled for Desktop-Only Build)</h2>
      <p>This example has been disabled as responsive components have been removed for the desktop-only build.</p>
    </div>
  );
};

export default ResponsiveLayoutExample;
