import React from 'react';

/**
 * Example component demonstrating the ResponsiveChessboardContainer
 * Shows how the chessboard adapts to different screen sizes
 */
const ResponsiveChessboardExample: React.FC = () => {
  return (
    <div style={{ padding: '20px' }}>
      <h2>Responsive Chessboard Example (Disabled for Desktop-Only Build)</h2>
      <p>This example has been disabled as responsive components have been removed for the desktop-only build.</p>
    </div>
  );
};

export default ResponsiveChessboardExample;
